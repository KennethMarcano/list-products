/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {  
        domains: ['dummyimage.com', 'images.unsplash.com', 'via.placeholder.com', 'storage.googleapis.com', 'your-bucket.s3.amazonaws.com', 'miro.medium.com', 'i.imgur.com'],  
      },  
};

export default nextConfig;
